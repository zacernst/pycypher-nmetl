version: "1.0"

sources:
  entities:
    - id: persons
      uri: "file:///data/persons.csv"
      entity_type: Person

functions:
  - module: "mypackage.string_utils"
    names:
      - normalize_name
      - parse_phone_number
