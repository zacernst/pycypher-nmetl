# fastopendata

Open data ingestion and processing utilities for the PyCypher ecosystem.

## Overview

`fastopendata` provides tools for loading public datasets and transforming
them into pycypher-compatible graph structures for Cypher query execution.

## Installation

Install via the workspace:

```bash
uv sync
```

## Usage

```python
from fastopendata import ...
```
